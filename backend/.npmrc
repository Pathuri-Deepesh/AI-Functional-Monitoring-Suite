# Ensure devDependencies (tsx, typescript) install on the server even if
# Elastic Beanstalk sets NODE_ENV=production. The app runs TypeScript directly
# via tsx at runtime, so these are required in "production".
production=false
